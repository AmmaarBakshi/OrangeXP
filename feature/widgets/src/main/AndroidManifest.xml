<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Widgets refresh when the data layer reports a change (see WidgetRefresher),
         so updatePeriodMillis is 0: no extra wake-ups. -->
    <application>
        <receiver
            android:name=".ScoreWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_score_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_score_info" />
        </receiver>

        <receiver
            android:name=".StreakWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_streak_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_streak_info" />
        </receiver>

        <receiver
            android:name=".TimetableWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_timetable_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_timetable_info" />
        </receiver>

        <receiver
            android:name=".StatsWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_stats_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_stats_info" />
        </receiver>
    </application>
</manifest>
