<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the window background matters before Compose draws; the UI theme lives in core:designsystem. -->
    <style name="Theme.OrangeXP" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ox_background</item>
    </style>
</resources>
